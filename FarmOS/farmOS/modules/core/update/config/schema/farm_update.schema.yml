farm_update.settings:
  type: config_object
  label: 'farmOS Update settings'
  mapping:
    managed_config:
      type: sequence
      label: 'Configuration items that should be automatically updated'
      sequence:
        type: string
        label: 'Configuration item'
